# ---- LangChain 1.x 全家桶（主版本锁死，避免破坏性变更）----
langchain~=1.2.0
langchain-core~=1.3.0
langchain-openai~=1.2.0           # 默认 provider：Volcengine Doubao 走 OpenAI 兼容协议
langchain-anthropic~=1.4.0        # 保留，便于切回 Claude
langchain-community~=0.4.0
langchain-chroma~=1.1.0
langchain-classic~=1.0.0          # 旧版 ConversationBufferMemory 等仍住在 classic
langgraph~=1.1.0
langgraph-prebuilt~=1.0.0

# ---- 向量库 ----
chromadb~=1.5.0

# ---- Embedding（本地 sentence-transformers）----
sentence-transformers~=5.4.0
langchain-huggingface~=1.2.0      # 替代已废弃的 langchain_community.embeddings.HuggingFaceEmbeddings
# tokenizers / transformers / huggingface_hub 由 sentence-transformers 拉

# ---- Web 服务 ----
fastapi~=0.136.0
uvicorn[standard]~=0.46.0
sse-starlette~=3.4.0

# ---- 基础 ----
python-dotenv~=1.2.0
pydantic~=2.13.0
